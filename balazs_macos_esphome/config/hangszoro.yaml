esphome:
  name: hangszoro
  friendly_name: hangszoro

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:
  level: VERY_VERBOSE
# Enable Home Assistant API
api:
  encryption:
    key: "KexYxZB1DdMVvMu532X6mQ7k+VZg6D+7C/Bq7JJNM5c="

ota:
  - platform: esphome
    password: "62c40dd27e8b701dff831ce048fc74c7"
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  manual_ip:
    static_ip: 192.168.22.182
    gateway: 192.168.22.1
    subnet: 255.255.255.0

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "hangszoro"
    password: "0f4c873EwxHU"

captive_portal:



web_server:
  port: 80


i2s_audio:
  i2s_lrclk_pin: GPIO22
  i2s_bclk_pin: GPIO26
  id: i2s_out

media_player:
  - platform: i2s_audio
    name: ESPHome I2S Media Player
    dac_type: external
    i2s_audio_id: i2s_out
    i2s_dout_pin: GPIO25
    mode: mono

switch:
  - platform: restart
    name: "hangszoro Restart"